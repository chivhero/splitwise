# 🤖 Настройка Telegram Бота SplitWisedbot

## ✅ Конфигурация создана!

Ваши данные бота:
- **Имя:** `@SplitWisedbot`
- **Токен:** `8533199204:AAG4CZJAuw1vsfQ8RbOh6Nf-jIg-roUZAY8`

## 🚀 Быстрый старт

### 1. Файлы уже созданы:

✅ `.env.local` - локальная конфигурация (с вашим токеном)  
✅ `.env.example` - шаблон для других разработчиков

### 2. Настройка Web App в BotFather

Откройте [@BotFather](https://t.me/BotFather) и выполните:

```
/mybots
→ @SplitWisedbot
→ Bot Settings
→ Menu Button
→ Configure menu button
```

**URL для локального тестирования (через ngrok):**
```
https://YOUR_NGROK_URL.ngrok.app
```

**URL для production (после деплоя на Vercel):**
```
https://your-app.vercel.app
```

### 3. Запуск приложения

```bash
# Установите зависимости (если ещё не установлены)
npm install

# Запустите сервер разработки
npm run dev
```

Приложение запустится на `http://localhost:3000`

### 4. Тестирование через ngrok

Для тестирования Telegram Web App на локальной машине:

```bash
# Установите ngrok (если ещё нет)
brew install ngrok  # macOS
# или скачайте с https://ngrok.com

# Запустите туннель
ngrok http 3000
```

Скопируйте HTTPS URL (например, `https://abc123.ngrok.app`) и обновите в BotFather.

### 5. Настройка команд бота (опционально)

В BotFather:

```
/mybots
→ @SplitWisedbot
→ Edit Commands

Вставьте:
start - Открыть приложение
help - Справка
premium - Информация о Premium
```

## 🌐 Открытие бота

После настройки откройте бота в Telegram:

👉 **https://t.me/SplitWisedbot**

Нажмите `/start` и откроется ваше Web App!

## 📝 Переменные окружения

Текущие переменные в `.env.local`:

```env
TELEGRAM_BOT_TOKEN=8533199204:AAG4CZJAuw1vsfQ8RbOh6Nf-jIg-roUZAY8
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=SplitWisedbot
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

**Для production** (Vercel):
1. Зайдите в настройки проекта на Vercel
2. Environment Variables
3. Добавьте те же переменные
4. Измените `NEXT_PUBLIC_APP_URL` на ваш production URL

## 🔒 Безопасность

⚠️ **ВАЖНО:**
- `.env.local` уже в `.gitignore` ✅
- Никогда не коммитьте токены в Git
- Для других разработчиков используйте `.env.example`

## 🎯 Следующие шаги

1. ✅ Конфигурация создана
2. [ ] Настройте Web App URL в BotFather
3. [ ] Запустите `npm run dev`
4. [ ] Протестируйте через ngrok
5. [ ] Задеплойте на Vercel
6. [ ] Обновите URL в BotFather на production

## 🐛 Troubleshooting

### Ошибка: "Telegram WebApp не инициализируется"

**Решение:** Убедитесь, что:
1. Web App URL настроен в BotFather
2. Используете HTTPS (или ngrok для локального теста)
3. Открываете через Telegram, а не браузер

### Ошибка: "Environment variables not found"

**Решение:**
```bash
# Проверьте, что файл существует
ls -la .env.local

# Перезапустите dev server
npm run dev
```

### Bot не отвечает

**Решение:**
1. Проверьте токен в `.env.local`
2. Убедитесь, что бот не заблокирован BotFather
3. Проверьте, что токен активен

## 📚 Полезные ссылки

- [Telegram Web Apps Documentation](https://core.telegram.org/bots/webapps)
- [BotFather Commands](https://core.telegram.org/bots#botfather)
- [Next.js Environment Variables](https://nextjs.org/docs/basic-features/environment-variables)
- [ngrok Documentation](https://ngrok.com/docs)

---

**Готово! Ваш бот @SplitWisedbot настроен и готов к запуску! 🎉**

Для запуска выполните:
```bash
npm run dev
```

Затем откройте https://t.me/SplitWisedbot в Telegram!

