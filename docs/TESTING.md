# 🧪 Руководство по тестированию SplitWise

## 📋 Оглавление

- [Установка](#установка)
- [Запуск тестов](#запуск-тестов)
- [Структура тестов](#структура-тестов)
- [Написание тестов](#написание-тестов)
- [Best Practices](#best-practices)

## 🚀 Установка

Все необходимые зависимости уже установлены! Если нужно переустановить:

```bash
npm install --save-dev jest jest-environment-jsdom @testing-library/react @testing-library/jest-dom @testing-library/user-event @types/jest
```

### ⚠️ Важно: Версия Node.js

Проект требует **Node.js 20.x** (не 24 или 25), так как `better-sqlite3` не поддерживает более новые версии.

Используйте nvm для управления версиями:

```bash
# Установить и использовать Node.js 20
nvm install 20
nvm use 20

# Установить Node.js 20 по умолчанию
nvm alias default 20
```

## 🧪 Запуск тестов

### Все тесты
```bash
npm test
```

### Watch режим (тесты перезапускаются при изменениях)
```bash
npm run test:watch
```

### Coverage (покрытие кода)
```bash
npm run test:coverage
```

## 📁 Структура тестов

```
project/
├── lib/
│   ├── calculator.ts
│   └── __tests__/
│       └── calculator.test.ts       # Unit тесты для логики расчетов
├── components/
│   ├── SettlementsView.tsx
│   └── __tests__/
│       └── SettlementsView.test.tsx # Тесты React компонентов
└── app/
    └── api/
        └── __tests__/               # API тесты (будущие)
```

## 📝 Типы тестов

### 1. Unit тесты (lib/__tests__/)

Тестируют изолированную бизнес-логику без зависимостей.

**Пример: calculator.test.ts**
- ✅ Расчет балансов
- ✅ Минимизация транзакций
- ✅ Обработка edge cases
- ✅ Округление чисел

### 2. Component тесты (components/__tests__/)

Тестируют React компоненты с mock данными.

**Пример: SettlementsView.test.tsx**
- ✅ Рендеринг компонента
- ✅ Состояния загрузки
- ✅ Обработка ошибок
- ✅ Взаимодействие с API (mock)

### 3. Integration тесты (планируются)

Тестируют полные пользовательские сценарии.

## 🔨 Написание тестов

### Unit тест

```typescript
import { calculateBalances } from '../calculator';

describe('calculateBalances', () => {
  it('должен правильно рассчитывать балансы', () => {
    const expenses = [
      {
        id: 'exp1',
        amount: 300,
        paidBy: 'user1',
        splitBetween: ['user1', 'user2', 'user3'],
        // ...другие поля
      },
    ];
    
    const balances = calculateBalances(expenses, users);
    
    expect(balances.find(b => b.userId === 'user1')?.balance).toBe(200);
  });
});
```

### Component тест

```typescript
import { render, screen, waitFor } from '@testing-library/react';
import MyComponent from '../MyComponent';

describe('MyComponent', () => {
  it('должен отображать данные', async () => {
    // Mock API
    global.fetch = jest.fn().mockResolvedValue({
      json: async () => ({ data: 'test' }),
    });
    
    render(<MyComponent />);
    
    await waitFor(() => {
      expect(screen.getByText('test')).toBeInTheDocument();
    });
  });
});
```

## ✅ Best Practices

### 1. Именование тестов

Используйте понятные описания на русском:

```typescript
// ✅ Хорошо
it('должен показывать ошибку при пустом поле')

// ❌ Плохо
it('test error')
```

### 2. Arrange-Act-Assert паттерн

```typescript
it('должен рассчитывать сумму', () => {
  // Arrange - подготовка данных
  const expenses = [...]
  
  // Act - выполнение действия
  const result = calculateSum(expenses)
  
  // Assert - проверка результата
  expect(result).toBe(300)
})
```

### 3. Тестируйте поведение, а не реализацию

```typescript
// ✅ Хорошо - тестируем результат
expect(screen.getByText('Успех')).toBeInTheDocument()

// ❌ Плохо - тестируем внутреннюю структуру
expect(component.state.isSuccess).toBe(true)
```

### 4. Изолируйте тесты

Каждый тест должен быть независимым:

```typescript
beforeEach(() => {
  jest.clearAllMocks() // Очищаем моки перед каждым тестом
})
```

### 5. Используйте моки для внешних зависимостей

```typescript
// Mock fetch API
global.fetch = jest.fn()

// Mock Telegram WebApp (уже в jest.setup.js)
global.Telegram.WebApp.ready = jest.fn()
```

## 🎯 Что тестировать?

### ✅ Обязательно тестируйте:

- Критическую бизнес-логику (алгоритмы расчетов)
- Обработку ошибок
- Edge cases (пустые массивы, нулевые значения)
- Пользовательские сценарии

### ⚠️ Не обязательно тестировать:

- Простые геттеры/сеттеры
- Стили и CSS
- Сторонние библиотеки
- Тривиальный код

## 📊 Coverage (Покрытие кода)

Цель: **80%+ покрытия** для критических модулей.

```bash
npm run test:coverage
```

Результат показывает:
- **Statements**: покрытие выражений
- **Branches**: покрытие ветвлений (if/else)
- **Functions**: покрытие функций
- **Lines**: покрытие строк кода

## 🐛 Отладка тестов

### Запустить конкретный тест

```bash
npm test -- calculator.test.ts
```

### Запустить в debug режиме

```bash
node --inspect-brk node_modules/.bin/jest --runInBand
```

### Использовать console.log

```typescript
it('тест', () => {
  console.log('Debug:', data)
  expect(data).toBeDefined()
})
```

## 🔧 Конфигурация

### jest.config.js

Основная конфигурация Jest для Next.js проекта.

### jest.setup.js

Настройки окружения:
- Моки для Telegram WebApp
- Моки для window.matchMedia
- @testing-library/jest-dom matchers

## 📚 Полезные ресурсы

- [Jest Documentation](https://jestjs.io/docs/getting-started)
- [React Testing Library](https://testing-library.com/docs/react-testing-library/intro/)
- [Testing Library Queries](https://testing-library.com/docs/queries/about)
- [Jest Matchers](https://jestjs.io/docs/expect)

## 🎓 Примеры запросов

### Поиск элементов

```typescript
// По тексту
screen.getByText('Нажми меня')

// По роли
screen.getByRole('button', { name: 'Отправить' })

// По label
screen.getByLabelText('Email')

// По placeholder
screen.getByPlaceholderText('Введите текст')

// По test-id (если добавлен data-testid)
screen.getByTestId('submit-button')
```

### Асинхронные тесты

```typescript
// waitFor - ждать пока условие станет true
await waitFor(() => {
  expect(screen.getByText('Загружено')).toBeInTheDocument()
})

// findBy - комбинация getBy + waitFor
const element = await screen.findByText('Загружено')
```

## 💡 Советы

1. **Пишите тесты параллельно с кодом** - это помогает найти баги раньше
2. **Не гонитесь за 100% coverage** - качество важнее количества
3. **Тестируйте edge cases** - именно там обычно прячутся баги
4. **Используйте describe блоки** - для группировки связанных тестов
5. **Читайте ошибки внимательно** - Jest дает очень подробные сообщения

---

## 🎉 Текущее состояние

✅ **17/17 тестов проходят успешно!**

- ✅ 11 unit тестов для алгоритма расчетов
- ✅ 6 component тестов для SettlementsView

**Следующие шаги:**
- Добавить тесты для других компонентов
- Добавить API тесты
- Настроить CI/CD для автоматического запуска тестов

---

*Happy Testing! 🧪*

