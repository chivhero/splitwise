# 🔄 Обновление существующего проекта на Vercel

## ✅ Git готов!

```
✓ 75 файлов добавлено
✓ 19,911 строк кода
✓ Коммит создан
✓ Готово к деплою
```

---

## 🚀 Шаг 1: Войдите в Vercel

```bash
source activate.sh
vercel login
```

Выберите метод входа (тот же аккаунт, где старый проект).

---

## 🔗 Шаг 2: Свяжите с существующим проектом

```bash
vercel link
```

**Ответьте на вопросы:**

1. **Set up and deploy?** → `Y`
2. **Which scope?** → Выберите ваш аккаунт
3. **Link to existing project?** → **`Y`** ✅ (важно!)
4. **What's the name of your existing project?** → Введите имя (например: `splitwise-telegram`)

Vercel найдёт ваш старый проект и свяжет с ним.

---

## 🚀 Шаг 3: Задеплойте рабочую версию

```bash
vercel --prod
```

Это обновит ваш существующий проект новым кодом!

---

## 📊 После деплоя

Vercel покажет:
```
✓ Production: https://ваш-проект.vercel.app
```

Этот URL **не изменится** - это тот же URL, что был раньше!

---

## ⚙️ Шаг 4: Проверьте переменные окружения

1. Откройте: https://vercel.com/dashboard
2. Ваш проект → Settings → Environment Variables
3. Убедитесь, что есть:

```env
TELEGRAM_BOT_TOKEN=8533199204:AAG4CZJAuw1vsfQ8RbOh6Nf-jIg-roUZAY8
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=SplitWisedbot
NEXT_PUBLIC_APP_URL=https://ваш-проект.vercel.app
```

4. Если чего-то нет - добавьте
5. Если изменили - нажмите **Redeploy**

---

## 🗄️ Шаг 5: База данных (PostgreSQL)

⚠️ **Важно:** На Vercel нужен PostgreSQL (SQLite не работает на serverless)

### Вариант A: Vercel Postgres

1. В Dashboard → Storage → Create Database → Postgres
2. Переменные добавятся автоматически
3. Redeploy проект

### Вариант B: Уже есть база данных

Если у вас уже настроена БД в старом проекте:
- ✅ Переменные должны остаться
- ✅ Данные сохранятся
- ✅ Ничего не нужно менять

---

## 🎯 Быстрый деплой (одна команда)

```bash
source activate.sh && vercel link && vercel --prod
```

---

## ✅ Проверка после деплоя

1. Откройте URL проекта
2. Проверьте, что приложение работает
3. Откройте @SplitWisedbot в Telegram
4. Убедитесь, что Web App открывается

---

## 🐛 Troubleshooting

### Проект не найден

**Решение:**
```bash
# Вручную укажите имя проекта
vercel --scope your-team-name --prod
```

### Ошибка билда

**Решение:**
```bash
# Проверьте локально
npm run build

# Если работает - проверьте логи на Vercel
```

### База данных не работает

**Решение:**
- Создайте Vercel Postgres (см. Шаг 5)
- Или подключите Supabase/другую PostgreSQL

---

## 📝 Что произойдёт

✅ Старый проект **обновится** новым кодом  
✅ URL **не изменится**  
✅ Переменные окружения **сохранятся**  
✅ База данных **останется** (если была)  
✅ Старые деплои **сохранятся** в истории  

---

**🚀 Готово! Запустите:**

```bash
source activate.sh
vercel login
vercel link
vercel --prod
```

